You are working in the Lumenia (Mentora) codebase on Replit.

GOAL
Add a new interactive + practice topic: “Operations with Surds” using the existing topic-aware template-based question engine. Do not redesign the engine. Follow the same schema and integration pattern as previous topics.

--------------------------------------------------------------------
1) TOPIC REGISTRY ENTRY (SINGLE SOURCE OF TRUTH)

Add to the central topic registry/config used by Interactive Topics, Practice, Warm-up and Exit Ticket:

{
  id: "operations_with_surds",
  slug: "operations-with-surds",
  title: "Operations with Surds",
  subject: "math",
  hasInteractive: true,
  hasPractice: true,
  generatorKey: "operations_with_surds"
}

--------------------------------------------------------------------
2) GENERATOR MODULE

Create:
  src/question_engine/operations_with_surds.py

Export:
  - generate_question(difficulty: str, seed: Optional[int] = None) -> dict
  - generate_pool(difficulty: str, n: int, seed: Optional[int] = None, ensure_unique: bool = True) -> list[dict]

Use random.Random(seed) for deterministic generation.

--------------------------------------------------------------------
3) QUESTION SCHEMA (MUST MATCH EXISTING GENERATORS)

Every generated question MUST follow EXACT schema:

{
  "id": "<deterministic short hash based on difficulty + archetype + params>",
  "topic": "operations_with_surds",
  "difficulty": "easy|medium|hard|challenge",
  "archetype": "<string>",
  "prompt": "<question text>",
  "answer": "<final simplified answer as a string>",
  "worked_solution": ["Step 1", "Step 2", "...", "Final simplified form"],
  "metadata": {
      "params": {...},
      "skills": [...],
      "estimated_time_sec": <int>
  }
}

ID rules:
- Deterministic from (difficulty + archetype + sorted params)
- No duplicates in a pool; retry up to 50 attempts when ensure_unique=True

Output formatting:
- Use a consistent surd format aligned to the app:
  Prefer "sqrt(n)" if the app uses that; otherwise use "√n".
- Do NOT output decimals.

--------------------------------------------------------------------
4) CONTENT SCOPE: OPERATIONS WITH SURDS

This topic focuses on:
- Addition and subtraction of surds (combining like surds)
- Multiplication of surds (including coefficients)
- Simple expansion involving surds (distributive property)
- Intro to conjugates / difference of squares (higher tiers)
Rationalising denominators is NOT the main focus here (keep it out or only 1–2 challenge items if your curriculum expects it here).

Core rules to test:
- Only like surds combine (after simplification)
- sqrt(a)*sqrt(b) = sqrt(ab)
- (sqrt(a) + sqrt(b))^2 expansion patterns

--------------------------------------------------------------------
5) ARCHETYPES & DIFFICULTY BANDS (Cambridge-style)

EASY (8)
Archetypes:
- add_like: k*sqrt(n) + m*sqrt(n)
- sub_like: k*sqrt(n) - m*sqrt(n)
- multiply_simple: sqrt(a)*sqrt(b) where ab is not a perfect square OR is a perfect square sometimes
Constraints:
- Use small n (2,3,5,6,7,10,11,13) for like terms
- Keep coefficients small (1..9)
Skills tags examples:
- ["combine_like_surds"]
- ["multiply_surds"]

MEDIUM (8)
Archetypes:
- add_after_simplify: k*sqrt(N) + m*sqrt(M) where both simplify to the same sqrt(base)
  Example: 3*sqrt(8) + 2*sqrt(18) -> 6sqrt(2) + 6sqrt(2)
- sub_after_simplify: 4*sqrt(12) - sqrt(27)
- multiply_with_coeff: (k*sqrt(a))*(m*sqrt(b)) -> (km)*sqrt(ab) then simplify
Constraints:
- At least 4 questions must require simplification BEFORE combining
- Ensure some end as integer (e.g., sqrt(12)*sqrt(3)=sqrt(36)=6) occasionally
Skills tags:
- ["simplify_surd", "combine_like_surds"]
- ["multiply_surds", "simplify_under_root"]

HARD (7)
Archetypes:
- expand_linear: (k + m*sqrt(a))*(p + q*sqrt(a)) then simplify (same surd base)
- expand_mixed: (k + sqrt(a))*(k - sqrt(a)) (difference of squares)
- simplify_expression: multi-step with +, -, × combined
Constraints:
- Keep arithmetic manageable; answers must be exact simplified surd forms
- Include at least 2 difference-of-squares items
Skills tags:
- ["expand_brackets", "combine_like_surds", "difference_of_squares"]
- ["simplify_expression"]

CHALLENGE (7)
Archetypes:
- square_binomial: (sqrt(a) + sqrt(b))^2 or (k + sqrt(a))^2 then simplify
- product_conjugates: (sqrt(a) + sqrt(b))*(sqrt(a) - sqrt(b))
- multi_term: k*sqrt(N) + m*sqrt(M) - t*sqrt(P) (some combine, some don’t)
Constraints:
- Must require multiple steps (simplify -> expand -> combine)
- Provide worked_solution that shows structure clearly
Skills tags:
- ["expand_brackets", "combine_like_surds", "simplify_expression", "structure_recognition"]

--------------------------------------------------------------------
6) WORKED SOLUTION QUALITY RULES

worked_solution must show:
- Any simplification of surds first (extract square factors)
- Combination of like surds (explicitly state “like surds combine”)
- For multiplication: multiply coefficients and multiply radicands
- For expansion: show FOIL/distribution and then simplify & combine

Example solution style:
- "sqrt(8)=2sqrt(2), sqrt(18)=3sqrt(2)"
- "3sqrt(8)+2sqrt(18) = 3(2sqrt(2)) + 2(3sqrt(2))"
- "= 6sqrt(2) + 6sqrt(2) = 12sqrt(2)"

No decimals. Keep exact forms.

--------------------------------------------------------------------
7) PRACTICE + WARM-UP + EXIT INTEGRATION

Do NOT hardcode topics.
Assume Warm-up and Exit Ticket are already topic-aware via central registry + generator resolver.

Ensure:
- Practice page loads this topic using generatorKey "operations_with_surds"
- Header shows "Math – Operations with Surds"
- Regenerate works (new seed)

Default difficulty mix for Practice:
- 3 easy + 3 medium + 2 hard
Optional: Challenge toggle off by default

Warm-up default:
- 2 easy + 2 medium

Exit ticket default:
- 1 medium + 1 hard

--------------------------------------------------------------------
8) TESTS

Add:
  tests/test_operations_with_surds_generator.py

Verify:
- required keys exist
- seeded determinism
- uniqueness in pool
- at least one MEDIUM question requires simplify-before-combine
- at least one HARD or CHALLENGE includes bracket expansion
- answers are exact (no decimals)

--------------------------------------------------------------------
9) DOCUMENTATION

Update:
  docs/question_engine.md
Add entry for "Operations with Surds" and note formatting conventions used.

--------------------------------------------------------------------
DO NOT

- Do not change DB schema
- Do not introduce new dependencies
- Do not output decimal approximations
- Do not redesign engine or break existing topics

DELIVERABLES
- Topic registry entry
- Generator module for operations_with_surds
- Practice works end-to-end
- Tests passing
- Docs updated
